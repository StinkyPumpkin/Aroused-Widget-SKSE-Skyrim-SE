#pragma once

// Registers the "HUD Widgets" section in SKSE Menu Framework's Mod Control
// Panel with sliders for position/scale/enable per widget plus cadence.
namespace SettingsUI {
    void Register();
}
